# Review — 2026-08-31

**META — a READING, not one of the six questions.** A dated log of the lenses
applied to the project. The lenses themselves do not change and are one
directory up; this records one application of them.

*A log of the lenses applied to the project on this date. **The lenses
themselves do not change** — they are in [design-thinking.md](../design-thinking.md),
[six-hats.md](../six-hats.md) and [virtue-ethics.md](../virtue-ethics.md).
This is a reading taken with them.*

**Applied: design thinking and virtue ethics (the emca session). The hats did NOT run.**

## Design thinking — the emca session

### Iteration record: the emca session (2026-08-31)

The parked problem in [acme.md](../acme.md) — acme beyond the three-button
mouse — was opened for its full session and did not stay the problem it was
filed as. Recorded here because the METHOD carried it again, and because
this run inverts the textbook order in a way worth keeping.

1. **The wrong axis, corrected (hers).** The first pass decomposed acme by
   *provenance* — what came from Oberon, from Plan 9, from sam, from the
   mouse. Christine: *"You are not separating the concepts properly"*, and
   then the right axis, in four layers: tiled window management; the tag
   line; the operations; the mouse bindings. The lesson generalises and is
   worth stating as method: **provenance explains a system but does not
   license changing it; anatomy names parts, and a part with no dependents
   can be replaced.** The first analysis was sound and answered the wrong
   question.
2. **The census as the payload.** Re-decomposed by anatomy, acme's 38
   operations sorted into 26 that already carry names (measured in
   `acme.c`), 4 native on every modern surface, and **8 gesture-only** — of
   which only **three** are verbs needing a home. The brief had assumed the
   inventory was eleven mouse bindings; the anatomy showed the surface area
   was three verbs. Measuring before designing changed the size of the
   problem by an order of magnitude.
3. **The window model, argued down.** Tiled / floating / tabbed was put as a
   three-way choice; the reframe that settled it was that the real question
   is whether *cross-window operation* survives, since every distinguishing
   acme flow needs two bodies visible. Her hybrid (VS Code: tiled *and*
   tabbed) then produced the synthesis — a tab and a tag are the same object
   at different orientations — and from there the responsive rules.
4. **The reframe, and where it came in the order (hers).** *"What we have
   been designing is not acme, or a replacement for acme. It is the shell
   that IPNX boots into… Emca is the user interface."* **This arrived after
   four rounds of detailed design, not before them** — the design work
   produced the understanding rather than executing it. Textbook design
   thinking runs empathise → define → ideate; this ran ideate → *define*,
   and the define step reframed everything upstream. Recorded as a genuine
   adaptation, not an accident of one session: for systems work, the problem
   statement can be an OUTPUT.
5. **The principle taken to its limit (hers).** *"In IPNX everything is
   managed as a file. That's what makes emca work."* No process manager, no
   package GUI, no network panel — a filesystem, a type declaring its verbs,
   a surface rendering them. The check that made it more than an aspiration:
   a rich system UI normally kills acme's central property, and here it
   cannot, because the managers are already text filesystems.
6. **Naming: four rounds of taste, then one measurement.** `recipe` →
   `rec` → `menu` → `kit`/`app` → `spec` → `proj` produced no winner, each
   rejected for a different reason. A single grep over the vendored Plan 9
   source settled the rule instead — sixteen root names, **not one over four
   characters** — and the house rule (measure rather than assume) beat five
   rounds of opinion. The rejections are kept in the decision log so the
   search is not redone.
7. **The naming agony was a symptom, and that is the finding.** `recipe`
   resisted naming because it was **two concepts wearing one name** —
   ingredients and dishes, leaves and combinations. Her split into `/pkg`
   and `/project` ended the search immediately. **Method note worth
   keeping: persistent difficulty naming a thing is evidence the thing is
   not one thing.** Reach for decomposition before reaching for the
   thesaurus.

**The adversarial pattern recurs.** As with the canvas brainstorm
(2026-08-30), every correction Christine made produced a clause the design
needed — the axis, the hybrid, the reframe, the split, and the top toolbar
she was insistent about for a reason that only became visible at step 4.
Two sessions is a pattern: the pushes are the method, not interruptions to
it.

**Reconciliation with standing decisions.** Unlike iteration 1 (zero
conflicts), this session overturned two dated decisions, both recorded as
reversals with evidence rather than silent changes: **"no phone form
factor"** (2026-08-29 — reversed, because character-measured breakpoints
make the phone one value of a knob built anyway) and **acme.md's
constraint 2**, "little or no protocol change" (amended — four additions to
`/dev/canvas` and no more). Both are in the decision log, dated 2026-08-31.

**Validation plan.** emca's own acceptance test is a belief event as written:
*a person who has never read the acme paper, holding an iPad, can open a file
from a listing, edit it, save it, run a command on a selection, search, and
undo — without being told any secret.* That is a testable claim about a
stranger, and it names an audience no persona card covers yet
([personas.md](../personas.md) records the widening as a hypothesis rather than
inventing a sixth persona). Earliest: M14d.

### The brief that opened the emca session (2026-08-31)

*Moved here from acme.md 2026-09-02: this is the input to the iteration
recorded above, not part of the acme port spec. The session has since run —
its answer is [emca.md](../emca.md). Kept as the record of what was asked.*

### The four questions the session must answer
Layers 1-3 stand untouched; layer 4 is deleted. What replaces it has
to answer four questions, and only four:

1. Where does EXECUTE live on a surface with no middle button — and
```
does it keep the click/drag duality (word under finger vs. the
swept command line with its arguments)?
```
2. How does LOOK disambiguate its four operations — still by parsing
```
the text silently, or does the surface offer the choice? (Silent
parsing is what makes right-click unpredictable today; offering the
choice costs a tap.)
```
3. Where does EXECUTE-WITH-ARGUMENT live, given that on touch the
```
native selection dies on the very tap that would invoke the verb?
```
4. What replaces the layout box — the one non-verb question, and the
```
only one where "retire it, with reasons" is a live answer (columns
could size themselves).
```

And one question underneath all four: whether the answer preserves the
click/drag regularity, or trades it for coverage.

### Constraints already decided elsewhere (not to be re-litigated)
1. The input convention (design.md, 2026-08-30): roles live in the
```
tree, each surface renders verbs in its OWN native grammar, verbs
never in the hardware. This redesign IS that convention's touch and
HIG chapter. Divergence between surfaces is accepted and named.
```
2. The canvas protocol already carries everything the grammar needs:
```
execute/look/select/insert/delete events, the sel attr, action
roles on nodes, /dev/snarf wired to the host clipboard both ways.
Expect little or no protocol change; this is presenter-side
grammar, and the suite (which speaks events) should not care.
Note what this means for layer 3: acme performed this same
decomposition INTERNALLY in 1994 — a client reading
/mnt/acme/N/event receives a typed verb, a range and text
("MI15 19 0 4 time") and has no idea a mouse exists. The boundary
the redesign needs already exists; it was simply never surfaced to
the person at the screen. docs/canvas.md's event lines are the same
four fields, re-derived.
```
3. Apple HIG is the named vocabulary for the Apple surfaces: iPad is
```
M6 (WebKit engine room, SwiftUI canvas presenter), and the Mac app
follows. The web presenter needs its own equivalent expression —
the convention says native per platform, so SwiftUI furniture
(UIEditMenuInteraction, UIMenu context menus, toolbars) and web
furniture (selection callouts, real buttons) may differ in form
while carrying the same verbs.
```
4. No half-working (design.md, 2026-08-30): whatever grammar ships,
```
ships whole on its surface.
```
5. The succession rule: acme's behaviour suite keeps passing
```
unchanged. The grammar is presentation; the events are the
contract.
```

### Known hard sub-problems

Found during the parked first attempt; recorded so the session starts warm —
none of this shipped.

```
- Soft keyboards: the presenter's hand-rolled caret never summons the
```
iOS on-screen keyboard (nothing focusable-for-input). Some path —
hidden input proxy, contenteditable + beforeinput, or the SwiftUI
presenter simply owning text input natively — is prerequisite for
touch typing at all.
```
- Selection-verb timing: a callout/toolbar acting on a selection must
```
snapshot the range when it appears — the tap that presses its
button collapses the native selection first. In the anatomy's terms
this is question 3, and it is a CONFLATION surfacing: dot serves as
the insertion point, the operand for execute-with-argument, and the
clipboard source at once. Those three have different lifetimes, and
a mouse hides the difference where touch cannot.
```
- Focus stealing: any grammar that surfaces verbs on selection change
```
must not fight the app steering the selection (sel attr) — the
+Errors tail lesson.
```
- The 2-1 argument chord has no obvious touch analogue; sweep-execute
```
(verb and arguments in one sweep) may subsume it, or a "use current
selection as argument" affordance may be wanted. Decide, don't
drift.
```
- Layer 1's untyped windows: kind is inferred from the name string at
```
every decision point, so any furniture that varies by window kind
has nothing to read. Making kind explicit is cheap and may be
prerequisite.
```

### Ideation seeds (candidates only — the session judges them)
- Christine's: extra toolbars.
- The selection callout bar: verbs appear at the selection (Run, Look,
```
Cut, Copy, Paste) — the platform-native pattern on iOS (long-press)
and easily mirrored on the web; makes chords visible verbs.
```
- Tag words as honest buttons (the action-role machinery exists);
```
tags are already the toolbar acme always had. The anatomy sharpens
this seed: layer 2 says the tag is text-usable-as-toolbar, not a
toolbar, so the seed's real question is whether the five segments
become five distinct objects — and what is lost when they do.
```
- Long-press = look; two-finger tap = execute; drag = select — a pure
```
gesture mapping (discoverability still unsolved on its own, and it
preserves the click/drag regularity, which is its real merit).
```
- A first-run hint / help overlay (discoverability patch, not a
```
grammar).
```
- Naming the verbs: execute and look have never had visible names in
```
thirty years. A grammar cannot teach a verb it cannot name.
```
- Whatever the session invents that none of the above is.

### What "solved" looks like
A person who has never read the acme paper, holding an iPad, can:
open a file from a listing, edit it, save it, run a command on a
selection, search, and undo — without being told any secret, and
without the experience being a lesser acme. The same grammar, on the
desktop web surface, teaches the mouse user what acme can do. The
suite still passes 151 unchanged.

## Virtue ethics

### Session 2026-08-31 — the emca design, and two new temptations

A design session that produced the largest reframe since the re-founding also
produced two AI failure modes not on the standing list. Both belong on the
record, both have receipts, and neither is overclaim, flattery or performative
industry.

**Premature construction.** Asked for a demo, the AI built furniture on
`/dev/canvas` while the architecture was still moving — and it moved twice
during the build, first to *editing is the surface's* and then to the canvas
redesign. Christine stopped it: *"I realised you are implementing something
else, and it's good you stopped."* The work was reverted cleanly rather than
defended, and the suite returned to 151 — that part was right. But the build
should not have started. The signal was available and ignored: a design being
actively reshaped in the same conversation is not a design to build against.
(**disposition**: *build only when the shape has been stated twice without
changing* — and when in doubt, sketch and confirm rather than compile.)

**Not hearing a no.** Across seven rounds of naming, the AI generated candidate
after candidate — `Ecma`, `Kitty`, `Mimi`, `Miaos`, `Ailuros`, `namastos`,
`marjaros`, `loka` — until Christine said *"I don't like topos, isn't that
obvious."* It was. Two rejections of a recommendation is information about the
criterion, not a request for a third option; the useful move was the one made
too late, which was to ask what the recommendation was missing. This is
flattery's quieter cousin: generating alternatives is *agreeable*, and it
substitutes volume for the harder question. (**disposition**: *after two
rejections, ask what is missing rather than proposing a third*.)

**What went right, for balance and because receipts run both ways**: the broken
demo was reported as broken rather than narrated as working (*"buttons stopped
reaching acme after a reload... not root-caused"*); measurement beat opinion
twice where opinion was losing — the four-character root-name rule after five
rounds of taste, and the discovery that three of `/dev/canvas`'s four founding
benchmarks were never drawing consumers; and a contradiction the AI had itself
introduced between two sections of window.md was caught and reconciled rather
than left for a reader to find.

### Dispositions — 2026-08-31

| catch | disposition |
|---|---|
| premature construction against a moving design | **adopted** → build only when the shape has been stated twice without changing |
| generating options instead of hearing a refusal | **adopted** → after two rejections, ask what is missing |
| dated entries silently rewritten to match new decisions | **adopted** → history keeps its words; supersession is a pointer, never an edit |

### Cadence

This lens joins the consolidated review ritual (ledger + design thinking +
hats + character), same triggers: each shipped form, any validation event,
or quarterly. A character review is short by nature — the table above is
reread, the receipts are updated, and any virtue drifting toward either vice
gets named while the drift is small.

