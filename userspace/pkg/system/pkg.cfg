# pkg.cfg — the system package's description, in ndb(6) (docs/packages.md).
# Plan 9's userland as this machine runs it: the commands vendored from
# 9legacy's sys/src/cmd, rc with its rcmain, and the libraries they link.
pkg=system version=2026.09.24
	description="Plan 9's userland: its commands and rc"
	source=9legacy
	sourcerev=433c37f
