emca &
sleep 1
echo -n one-line > /tmp/small
cat /rc/tests.rc > /tmp/big
echo -n 'big is '; echo `{wc -l < /tmp/big}^' lines'
w=`{cat /dev/window/edit/clone}
d=/dev/window/$w
echo -n /tmp/small > $d/content
sleep 1
echo 'rect 0 0 1200 800' > $d/wctl
echo newrow > $d/wctl
sleep 1
echo -n /tmp/small > $d/kids/0/content
echo -n /tmp/big > $d/kids/1/content
sleep 2
echo fit > $d/events
sleep 2
echo -n 'small (1 line):  '; cat $d/kids/0/wctl
echo -n 'big (many):      '; cat $d/kids/1/wctl
