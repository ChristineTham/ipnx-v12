# emca — the default workspace (docs/emca.txt PART SIX). What a namespace
# file will declare once boot is emca's; for now, two lines of rc, which is
# policy exactly where policy belongs.
rc /rc/emcaopen dir /usr/kitty >/dev/null
rc /rc/emcaopen text /etc/motd >/dev/null
