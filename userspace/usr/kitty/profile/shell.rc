#!/bin/rc
# /home/profile/shell.rc — kitty's, run by every new shell of theirs, after
# the system's /profile/shell.rc (docs/packages.md). Theirs to change. Plan 9
# has no counterpart: its user settings are in `$home/lib/profile`, which
# only a login shell reads.
